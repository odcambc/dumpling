$schema: "https://json-schema.org/draft/2020-12/schema"
description: DMS experiment definition CSV file
properties:
  sample:
    type: string
    description: Sample name
  condition:
    type: string
    description: sample condition that will be compared during differential expression analysis (e.g. a treatment, a tissue time, a disease)
  replicate:
    type: integer
    description: replicate number
  time:
    type: number
    description: timepoint of sample (for timecourse experiments)
  bin:
    type: integer
    description: sample bin (for FACS-type experiments)
  tile:
    type: integer
    description: tile number (for tiled amplicon experiments)
  file:
    type: string
    description: sample file name prefix (truncated at lane number)
  phenotype:
    type: integer
    description: >-
      optional cosmos phenotype slot for this sample's condition (1, 2, ...).
      Maps the condition's scores to cosmos beta_hat_N/se_hat_N. Must agree
      across all rows of a condition; leave blank to exclude from the cosmos
      export. Only used when run_cosmos is true.

required:
  - sample
  - condition
  - replicate
  - file

# Per-row enforcement of exactly-one-of {time, bin} happens in
# script_utils.validate_experiment_time_or_bin so the error message can name
# the offending sample. A schema-level oneOf reported only
# "is not valid under any of the given schemas".